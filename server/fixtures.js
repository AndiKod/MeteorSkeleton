// Feeding some data into Mongo via $ meteor reset

/*
if (Stuff.find().count() === 0) {
    
    Stuff.insert({
    title: 'Title1',
    foo: 'bar'
    });
    
    Stuff.insert({
    title: 'Title4',
    foo: 'bar'
    });
    
    Stuff.insert({
    title: 'Title3',
    foo: 'bar'
    });
    
}
*/